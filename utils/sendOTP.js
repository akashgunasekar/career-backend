// backend/utils/sendOTP.js
export default function sendOTP(phone, otp) {
  console.log(`📩 Sending OTP ${otp} to ${phone}`);

  // Later replace with actual SMS provider (Twilio / MSG91 / Firebase)
}
